# 維護紀錄

## 2026-05-07

- 初始化教師用教學套件 workspace。
- 建立共用目錄：`projects/`、`_templates/`、`_assets/`、`_docs/`、`archive/`。
- 建立根目錄 README、專案索引與小專案 README 模板。
- 建立第一個小專案：`projects/教師個人數位名片`。
- 依 Obsidian `LifeOS.md`、`CLAUDE.md`、`教學素材/Scratch 課程總覽.md` 整理第一版個人數位名片內容。
- 更新數位名片：加入儲三翔老師姓名、個人 Facebook 連結與半身照頭像。
- 更新數位名片：加入 5 年級動畫製作、5 年級遊戲製作、Scratch + AI 模組的 YouTube 播放清單連結。
- 更新數位名片：Facebook 按鈕改為同頁直接跳轉，並顯示 `facebook.com/chusanshine` 文字連結。
- 建立第二個小專案：`projects/網頁抽點器`。
- 更新根目錄 `index.html` 為專案列表，方便從 GitHub Pages 進入不同小專案。
- 更新網頁抽點器預設名單：從 `795952list.csv` 只取 `姓名` 欄位，公開版將中間字改為 `O`，未納入帳號與電子郵件。
- 更新網頁抽點器公開版：移除內建班級與學生名單，改用泛用範例名單，避免公開 repo 保存學生資訊。
- 更新網頁抽點器：更換 `localStorage` key 並清除舊 key，避免瀏覽器載入先前的班級名單快取。
- 更新網頁抽點器：加入 CSV 上傳功能，只取 `座號` 與 `姓名` 欄位，在瀏覽器本機產生抽點器。
- 更新網頁抽點器：上傳 CSV 時讀取 `班級` 欄位，動態顯示班級抽點器與班級學生名單標題。
- 更新網頁抽點器：CSV 匯入後依座號由小到大排序，顯示格式為「座號 姓名」。
- 建立第三個小專案：`projects/手機常用連結`，提供手機優先的三大按鈕入口。
- 更新手機常用連結：移除酷課雲，將學生雲端硬碟名稱改為快樂 e 學院，新增 Google 搜尋。
- 更新手機常用連結：快樂 e 學院網址改為 `https://i.ilc.edu.tw`。
- 更新手機常用連結：快樂 e 學院副標改為「學習入口平台」。
- 更新手機常用連結：新增第三列「學生常用連結」按鈕，連到 `https://reurl.cc/ovpE6M`。
- 建立第四個小專案：`projects/儀表板入口`，整合教師名片、抽點器與連結牆。

## 2026-05-09

- 建立小專案：`projects/隨機課堂鼓勵金句的網頁`。
- 隨機課堂鼓勵金句網頁初版：每次重新整理隨機顯示 1 則繁體中文鼓勵金句，採用大字置中與溫馨漸層背景。
- 將隨機課堂鼓勵金句網頁移入 `projects/`，並合併到 GitHub `main`。
- 擴充隨機課堂鼓勵金句清單：從 15 句增加到 50 句。
- 更新隨機課堂鼓勵金句網頁：加入抽籤桶互動，初始畫面顯示裝有多支籤的抽籤桶，點擊後桶身晃動並滾出一支籤，再顯示隨機金句。
- 調整抽籤桶版面：縮小抽籤桶與籤的高度，避免畫面裁切，並拉開提示文字與桶身距離。
- 更新隨機課堂鼓勵金句網頁：抽籤後隱藏抽籤桶，在原位置顯示黑板風格的「今日課堂鼓勵」與金句。
- 更新隨機課堂鼓勵金句網頁：在黑板正上方新增「重新抽籤」按鈕，可不重新整理瀏覽器就回到抽籤前畫面。
- 建立小專案：`projects/心情簽到小工具`。
- 心情簽到小工具初版：提供三個大心情按鈕，點擊後顯示對應鼓勵訊息與「今日簽到成功」。
- 更新心情簽到小工具：將三個心情按鈕改成心情文字雲，包含開心、緊張、平靜、疲累、難過、有信心、好奇、期待、需要幫忙、專心。
- 曾試作心情圖案雲版本，後依需求回復到文字雲版本。
- 更新心情簽到小工具：加入「今日心情統計」與「重新設置」按鈕。
- 調整心情簽到小工具統計顯示：一開始顯示「尚未有心情簽到」，只有被點過的心情詞才出現在統計區並顯示次數。
- 完成 GitHub 發佈流程，相關 PR 已合併：#1 到 #12。
- 今日成果頁：
  - `https://sanshinechu.github.io/teacher-teaching-kit/projects/隨機課堂鼓勵金句的網頁/`
  - `https://sanshinechu.github.io/teacher-teaching-kit/projects/心情簽到小工具/`

## 2026-05-09 收工紀錄

- 檢查 `projects/儀表板入口` 手機版版面，使用 320px、360px、375px、390px、430px 寬度確認沒有橫向跑版。
- 調整儀表板入口手機版工具卡片標題字級，讓長標題在窄螢幕上比較不擁擠。
- 修正 `projects/隨機課堂鼓勵金句的網頁` 手機版抽出金句後，黑板下方露出提示小文字的問題。
- 將隨機課堂鼓勵金句清單擴充為 200 句。
- 將金句抽選邏輯改為抽籤桶模式：一輪 200 句抽完前不重複，抽完後自動重新洗牌。
- 已完成手機版實測與 GitHub 上傳，commit：`b8c3d8c Update quote tool and mobile dashboard`。

## 2026-05-12

- 更新 `projects/儀表板入口` 工具卡片：在網頁抽點器、班級倒數計時器、隨機課堂鼓勵金句、心情簽到小工具加入「使用時機」說明。
- 調整工具卡片內的使用時機樣式，使用分隔線與較淡文字，讓卡片資訊更清楚。
- 補齊 `projects/儀表板入口` 教師數位名片與連結牆的「使用時機」說明。
- 調整 `projects/儀表板入口` 工具卡片排版，讓各卡片模組名稱從固定位置開始，視覺高度一致。
- 修正 `projects/儀表板入口` 班級倒數計時器卡片對齊問題，並替 CSS 加上版本參數避免瀏覽器套用舊樣式。

## 2026-05-13

- 建立小專案：`projects/AI 詠唱提示詞百寶箱`。
- AI 詠唱提示詞百寶箱初版：提供影像生成、劇本創作與學習任務三類提示詞卡片。
- 新增搜尋、分類篩選、卡片複製與右側編輯區，讓學生能先套用基礎提示詞再修改 `{變數}`。
- 更新根目錄 `index.html`、`_docs/project-index.md` 與 `projects/儀表板入口`，加入新工具入口。
- 建立小專案：`projects/運算思維流程畫布`。
- 運算思維流程畫布初版：提供開始、執行、判斷、輸入、輸出等流程圖形狀，可拖曳、改文字、連線、清空與下載 PNG。
- 更新根目錄 `index.html`、`_docs/project-index.md` 與 `projects/儀表板入口`，加入流程畫布入口。
- 更新 `projects/儀表板入口`：所有工具卡片連結改為開新分頁，並加上 `rel="noopener noreferrer"`。
- 修正 `projects/儀表板入口`：固定工具卡片標題與說明區列高，讓 AI 詠唱提示詞百寶箱與運算思維流程畫布的白色分隔線對齊。
- 建立小專案：`projects/班級作品牆`，提供 Padlet 風格的學生程式作品展示牆。
- 班級作品牆接入 Firebase Authentication 與 Firestore：學生使用 Google 帳號登入後，可在老師提供的班級連結中提交作品。
- Firestore 資料結構：`projectWallClasses/{classId}` 與 `projectWallClasses/{classId}/submissions/{submissionId}`，每個班級有獨立作品子集合。
- 權限設定：教師帳號可建立班級與刪除學生作品；學生可新增作品，但不能建立/切換班級，也不能刪除作品。
- 教師介面修正：未登入與學生登入時隱藏「建立或切換班級」區塊，避免學生看到教師管理功能。
- 縮圖功能：支援 Scratch 官方專題縮圖 API，並新增宜蘭縣 Scratch 平台 `scratch.ilc.edu.tw` 作品縮圖支援。
- 縮圖載入策略改為樂觀載入：先顯示圖片，失敗時才回退備用文字，修正預覽不顯示問題。
- 安全修正：Firebase Web API key 已從原始碼移除，改由 GitHub Actions secrets / variables 在部署時產生 `firebase-config.js`。
- GitHub Pages 已切換為 workflow 部署模式，最近部署狀態成功；線上 `firebase-config.js` 回應 `200`。
- 最新確認 commit：`bd80c8b 修正縮圖預覽不顯示：改為樂觀載入策略`，本機 `main` 已同步 `origin/main`。
- 重新設計 `projects/儀表板入口`：使用 `frontend-design` 方向改成可愛、溫暖、適合國小教學現場的工具入口版面。
- 儀表板入口新增主視覺摘要：9 個教學入口、3 種課堂情境、1 頁快速開啟。
- 儀表板入口樣式改為柔和多色卡片、貼紙感圓角、背景小花、星形、圓點與彩虹裝飾，保留 9 個工具入口與原有連結。
- 縮小主視覺高度與摘要列，讓使用者進入頁面後更早看到教學工具卡片。
- 使用 Chrome 檢查桌機 1280px 與手機 390px，確認沒有橫向跑版，9 個工具卡片皆正常顯示。
- 儀表板入口改為深色底圖參考版：使用深藍綠與墨色背景、柔和光暈與格線，保留可愛貼紙感。
- 調整工具卡片為深色半透明模組，保留各工具代表色條與圖示，讓整體與深色底圖一致。
- 儀表板主視覺資訊重整：將「羅東國小資訊組長」與「儲三翔老師」合併為單一身分標籤。
- 手機版微調：縮小右上「今日教學工具」標籤、壓縮身分標籤、調整主標題與說明文字，避免手機畫面擁擠。
- 已完成 GitHub 上傳，最新確認 commit：`5690adc 微調儀表板手機版排版`。

## 2026-05-14

- 手機版儀表板 `Teacher's Dashboard` 標籤字級改為 `0.74rem`，與右上角「今日教學工具」按鈕文字一致。
- 手機版「常用教學頁面集中入口，手機與電腦快速開啟。」說明文字改為 `display: none`，讓畫面更乾淨。
- commit：`2fa6ef2 手機版隱藏儀表板說明文字並統一標籤字級`，已推送到 GitHub `main`。
- 補償英中文光學大小差異，手機版 `Teacher's Dashboard` 字級逐步調至 `1rem`（中文字佔滿 em 格，英文需較大數值才視覺等高）。
- 電腦版「智慧教學儀表板」與「羅東國小資訊組長」列間距設為 `16px`（與上方 gap 一致）。
- 手機版同列間距調整至 `16px`（歷經多次微調）。
- CSS 版本號更新至 `v=20260514-2`，解決瀏覽器快取舊樣式問題。
- 最新 commit：`fa0b3e4`，已推送到 GitHub `main`。
- 建立小專案：`projects/AI音樂MV成果集`。
- AI 音樂 MV 成果集初版：提供作品展示牆、主打作品播放器區、分類篩選、創作工具標籤與課堂應用說明。
- 更新根目錄 `index.html`、`_docs/project-index.md` 與 `projects/儀表板入口`，加入 AI 音樂 MV 成果集入口。
- AI 音樂 MV 成果集加入 Google 登入管理功能：教師帳號登入後可在 Web UI 新增、編輯與刪除作品，資料存入 Firestore `aiMusicMvWorks`。
- 更新 Firestore rules：`aiMusicMvWorks` 公開讀取、僅 `shine@tmail.ilc.edu.tw` 可寫入，並驗證作品欄位格式。
- 已部署 Firestore rules 到 Firebase 專案 `study-b2e59`，規則編譯與 release 成功。
- AI 音樂 MV 成果集加入縮圖功能：可手動填入 `thumbnailUrl`，若作品是 YouTube 影片則自動產生影片縮圖。
